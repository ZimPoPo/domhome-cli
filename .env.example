# ───────────────────────────────────────────────
# domhome-cli – Environment Configuration
# Copy this file to .env and adjust values.
# ───────────────────────────────────────────────

# Serial port of the Zigbee dongle
# Windows:  COM5  (check Device Manager → Ports)
# Linux:    /dev/ttyUSB0 or /dev/ttyACM0
ZIGBEE_SERIAL_PORT=COM5

# Baud rate – 115200 is the default for ZBDongle-E (EZSP)
ZIGBEE_BAUD_RATE=115200

# Adapter type – "ember" for Silicon Labs (ZBDongle-E, firmware ≥ 7.4.x),
# "ezsp" for legacy firmware (< 7.4.x, deprecated), "zstack" for TI (ZBDongle-P)
ZIGBEE_ADAPTER=ember

# Path to the Zigbee database file (persistent device storage)
ZIGBEE_DB_PATH=./data/zigbee.db

# Log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info
